[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npx prisma generate --config prisma.config.ts",
  "npx @nestjs/cli build"
]

[start]
cmd = "node dist/main"
