# ──────────────────────────────────────────────────────────────
# VendaCore Frontend — Variables de Entorno
# Copia este archivo como .env.local para desarrollo local
# ──────────────────────────────────────────────────────────────

# URL del backend en producción (Railway).
# En desarrollo NO se necesita (el proxy de Vite maneja /api → localhost:4000)
# Ejemplo: VITE_API_URL=https://vendacore-api.railway.app/api/v1
VITE_API_URL=
